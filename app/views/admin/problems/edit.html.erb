<div class="page-header">
  <h1>Edit Problem</h1>
</div>

<%= render 'form' %>

<%= link_to 'Show', [:admin, @competition, @category, @problem], :class => 'btn' %> 
<%= link_to 'Back', [:admin, @competition, @category, :problems], :class => 'btn' %>
